import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'StudentDashboard',
  description: 'This is StudentDashboard for Startup Nextjs Template',
};

const StudentDashboard = () => {
  return <>hello</>;
};

export default StudentDashboard;

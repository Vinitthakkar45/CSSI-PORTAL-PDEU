apiVersion: 1

policies:
  - orgId: 1
    receiver: email-admins
    group_by: [alertname, severity]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 12h
    routes:
      - receiver: email-admins
        matchers:
          - severity =~ "warning|critical"
